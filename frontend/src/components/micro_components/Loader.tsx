import React from 'react';

const Loader = () => {
    return (
        <svg id='svg' viewBox="25 25 50 50">
            <circle id='circle' r={20} cy={50} cx={50} />
        </svg>
    )
}

export default Loader